# Railway Environment Template
# Copie os valores abaixo para as Variables do Railway (Project Settings -> Variables)

# Stripe Secret Key (obter em https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_test_YOUR_KEY_HERE

# Frontend URL (Railway fornecerá automaticamente)
FRONTEND_URL=https://your-app.railway.app

# Database URL (será fornecida automaticamente se usar Railway PostgreSQL)
# Se não usar, deixe este vazio - o código usa SQLite local como fallback
DATABASE_URL=postgresql://user:password@host:port/dbname
